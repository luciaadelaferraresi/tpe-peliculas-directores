<main class="container">
    <div class="section">
        <div class="section-header">
            <h1>Películas</h1>
            <?php if (!empty($_SESSION['EMAIL_USER'])): ?>
                <a href="<?= BASE_URL ?>peliculaForm" class="btn btn-primary btn-small">+ Agregar Película</a>
            <?php endif; ?>
        </div>

        <!-- Filtros -->
        <section class="filters section">
            <h3>Filtrar por Director</h3>
            <ul class="filter-list">
                <li><a href="<?= BASE_URL ?>peliculas" class="filter-btn">Todos</a></li>
                <?php foreach ($directores as $director): ?>
                    <li>
                        <a href="<?= BASE_URL ?>peliculas/<?= $director->id ?>" class="filter-btn">
                            <?= htmlspecialchars($director->nombre) ?>
                        </a>
                    </li>
                <?php endforeach; ?>
            </ul>
        </section>

        <!-- Tabla de películas -->
        <div class="section">
            <table class="data-table">
                <thead>
                    <tr>
                        <th>Título</th>
                        <th>Año</th>
                        <th>Director</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($peliculas as $pelicula): ?>
                        <tr>
                            <td><?= htmlspecialchars($pelicula->titulo) ?></td>
                            <td><?= htmlspecialchars($pelicula->anio) ?></td>
                            <td><?= htmlspecialchars($pelicula->director) ?></td>
                            <td class="btn-container">
                                <a href="<?= BASE_URL ?>pelicula/<?= $pelicula->id ?>" class="btn btn-view btn-small">Ver</a>
                                <?php if (!empty($_SESSION['EMAIL_USER'])): ?>
                                    <a href="<?= BASE_URL ?>editPelicula/<?= $pelicula->id ?>" class="btn btn-primary btn-small">Editar</a>
                                    <a href="<?= BASE_URL ?>deletePelicula/<?= $pelicula->id ?>" class="btn btn-delete btn-small">Eliminar</a>
                                <?php endif; ?>
                            </td>
                        </tr>
                    <?php endforeach; ?>
                </tbody>
            </table>
        </div>
    </div>
</main>
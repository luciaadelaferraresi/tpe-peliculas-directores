<main class="container detalle-container">
    <div class="detalle-card panel">
        <h1 class="detalle-titulo"><?= htmlspecialchars($pelicula->titulo) ?></h1>
        <ul class="detalle-info">
            <li><strong>Año:</strong> <?= htmlspecialchars($pelicula->anio) ?></li>
            <li><strong>Duración:</strong> <?= htmlspecialchars($pelicula->duracion) ?> min</li>
            <li><strong>Director:</strong> <?= htmlspecialchars($pelicula->director) ?></li>
        </ul>
        <div class="detalle-portada">
    <img src="<?= htmlspecialchars($pelicula->portada) ?>" 
         alt="Portada de <?= htmlspecialchars($pelicula->titulo) ?>">
</div>
        <a href="<?= BASE_URL ?>peliculas" class="btn btn-primary btn-small">← Volver atrás</a>
    </div>
</main>
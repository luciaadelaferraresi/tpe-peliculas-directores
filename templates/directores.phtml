<main class="container">
    <div class="section">
        <div class="section-header">
            <h1>Directores</h1>
            <?php if (!empty($_SESSION['EMAIL_USER'])): ?>
                <a href="<?= BASE_URL ?>directorForm" class="btn btn-small btn-primary">+ Agregar Director</a>
            <?php endif; ?>
        </div>

        <div class="section">
            <table class="data-table">
                <thead>
                    <tr>
                        <th>Nombre</th>
                        <th>Nacionalidad</th>
                        <th>Peliculas dirigidas</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($directores as $director): ?>
                        <tr>
                            <td><?= htmlspecialchars($director->nombre) ?></td>
                            <td><?= htmlspecialchars($director->nacionalidad) ?></td>
                            <td><?= htmlspecialchars($director->peliculas_count) ?></td>
                            <td class="btn-container">
                                <a href="<?= BASE_URL ?>director/<?= $director->id ?>" class="btn btn-small btn-view">Ver</a>
                                <?php if (!empty($_SESSION['EMAIL_USER'])): ?>
                                    <a href="<?= BASE_URL ?>editDirector/<?= $director->id ?>" class="btn btn-small btn-primary">Editar</a>
                                    <a href="<?= BASE_URL ?>deleteDirector/<?= $director->id ?>" class="btn btn-small btn-delete">Eliminar</a>
                                <?php endif; ?>
                            </td>
                        </tr>
                    <?php endforeach; ?>
                </tbody>
            </table>
        </div>
    </div>
</main>
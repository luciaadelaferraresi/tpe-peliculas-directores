<main class="form-page">
    <div class="form-container panel">
        <h2 class="form-title">Iniciar Sesión</h2>
        <p class="form-subtitle">Complete los siguientes campos para iniciar sesion</p>
        <?php if (!empty($error)): ?>
            <div class="alert alert-danger"><?= $error ?></div>
        <?php endif; ?>
        <form method="post" action="verify">
            <div class="form-group">
                <label for="email">Email</label>
                <input type="text" id="email" name="email" placeholder="tu@email.com" required>
            </div>
            <div class="form-group">
                <label for="password">Contraseña</label>
                <input type="password" id="password" name="password" placeholder="********" required>
            </div>
            <button type="submit" class="btn btn-primary btn-large">Ingresar</button>
        </form>
    </div>
</main>